# ReporTicket - Database Configuration

# Master Database Connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=reporticket_master

# cPanel specific (if required, e.g., 'username_')
DB_PREFIX=

# Frontend URL (for password reset links, etc.)
FRONTEND_URL=http://localhost:5173

# SMTP Configuration (Can also be moved here for better security)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=true
