# DATABASE CONFIGURATION
# Set DB_MODE to 'single' for cPanel shared hosting or 'multi' for VPS/Local
DB_MODE=single

# Host and Credentials
DB_HOST=51.77.71.235
DB_PORT=3306
DB_USER=capaorg_abrahammontes
DB_PASS=A06m09c79!.
DB_NAME=capaorg_ReporTicket

# Frontend URL (for password reset links, etc.)
FRONTEND_URL=http://localhost:5176

# EMAIL SERVER (SMTP)
# These will be updated from the Admin Panel, but can be set here as defaults
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=true
VITE_API_URL=http://localhost:3001
